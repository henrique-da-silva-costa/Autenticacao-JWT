# Autenticação Segura com JWT: React + Laravel 🛡️

## Recentemente desenvolvi um sistema de login completo focando em segurança e performance. O objetivo foi aplicar o fluxo de autenticação via JSON Web Tokens (JWT), garantindo uma comunicação segura entre o front-end e a API.

# O que usei no projeto:

## Front-end: React com TypeScript (tipagem estrita para evitar erros).

## Back-end: PHP com Laravel (construção de API REST).

## Segurança: Autenticação Stateless via JWT.
